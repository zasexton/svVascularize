# GUI-specific requirements for svVascularize
# Install with: pip install -r requirements-gui.txt

# Core Qt bindings
PySide6>=6.4.0

# PyVista and Qt integration (3D viewport)
pyvista>=0.41
pyvistaqt>=0.11.0

# VTK backend used by PyVista
vtk

# Numeric stack used heavily by the GUI
numpy>=1.24

# Optional: system monitoring in status bar
psutil>=5.9.0

# Optional: telemetry (crash reporting) backend
sentry-sdk>=1.0
